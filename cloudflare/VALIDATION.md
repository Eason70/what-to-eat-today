# Cloudflare 迁移验证

日期：2026-09-24。

- `npm test`：53 项通过，含 8 项 Worker 测试；覆盖关闭开关、凭据隔离、跨站阻断、参数限制、限流失败关闭、上游响应裁剪、兼容接口及错误脱敏。上游使用模拟响应，没有调用真实高德。
- `npm run build` 和 `npm run check:build`：通过，检查静态白名单、图标、缓存和真实服务端凭据排除。
- `npm run test:e2e`：23 项通过，含 320–1280px 布局、异步竞态、位置失败、门店排序展示和离线缓存。
- `wrangler deploy --dry-run`、`wrangler types`：通过。
- Wrangler 本地 workerd：配置接口正常，关闭状态返回 `AMAP_DISABLED`，CSP 生效，`.env`、服务器源文件和构建信息不可读取；390px 页面无脚本错误、横向溢出或外部请求，随机抽取及离线重开通过。页面截图已查看。
- 发布成功：`what-to-eat-today`，版本 `11ba9a4f-2dfa-42fd-a6ce-3b798cb70107`。上传 16 个静态资源，绑定为静态资源、限流器及 `AMAP_ENABLED=false`，无高德 Secret。
- 地址：https://what-to-eat-today.liu1214550793.workers.dev/。

- 用户真机反馈：用户在收到正式地址及“打开并点击随机一下”的验收请求后回复“正常”，确认正式页面的手机访问和随机抽取通过。

## 启用高德（2026-09-24）

- 所有者已明确允许使用现有密钥并承担可能产生的调用费用；没有购买套餐或充值。
- 三个高德凭据通过标准输入交给 Wrangler，保存为 Cloudflare Secret，不落入部署配置或公开源码。已设置 `AMAP_ENABLED=true`。
- 修复云端实际运行发现的兼容问题：Workers 不接受 `fetch` 的 `redirect:error`。改为 `redirect:manual`，对 3xx 响应返回上游错误，仍不跟随重定向。新增回归测试后，54 项单元及接口测试全部通过；前端资源未改动，沿用已有 23 项浏览器测试结果。
- 通过 Cloudflare 远程预览复现该错误，并确认修复后可连接高德；诊断没有使用真实 Key。远程预览已停止。
- 正式部署版本：`b1fde605-5339-47b4-b996-c0b10d2d200d`。
- 正式 API 验证：配置已启用；搜索“北京王府井”返回 20 个候选；以返回的王府井步行街坐标搜索 1km 内重庆小面得到 9 条原始候选；逆地理编码和 SDK 坐标转换成功。全部经正式 Worker 代理，使用公开地标，不读取用户实际位置。门店结果数量不代表菜品或营业情况已人工核验。
- 正式网页交互验证：搜索并选择“王府井步行街”，随机抽到“冒菜”，点击“附近哪有”后展示 5 家门店，包含距离、地址及高德 App/网页目的地链接。未替用户启动实际导航。
- 先前开发电脑的 TLS 握手问题在本次正式 API 和浏览器验证中未复现。
- 手机定位验收：用户按刷新正式页面并使用当前位置的步骤测试后，回复“定位正常”。

限制：高德 App 导航跳转仍待真机确认；账户剩余权益、计费明细及域名白名单配置未独立审阅。请求成功只能证明当前接口和凭据可用，不代表免费或费用封顶。每 IP 每分钟 20 次限流按 Cloudflare 节点分别计数，不是全站每日费用上限。
