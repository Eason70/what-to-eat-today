# 测试记录

日期：2026-09-24。本记录针对完整版项目的既有本地验证。

## 公开随机版发布记录

后续手机验收：用户反馈 Sites、EdgeOne 默认域名和 Netlify 测试地址均无法在手机网络打开，未完成公网访问验收。下方平台发布成功记录不能作为手机可访问的依据；简化版与其生成的截图不包含在完整版源码仓库中。

- 用户明确选择仅开放随机餐食，暂不开启付费查店。
- 独立发布目录：`public-site/`。公开网址：https://today-food-picker.queen11z.chatgpt.site 。Sites 发布状态 `succeeded`，访问模式 `public`，版本 1；源提交 `a4a935f7604675a77082b55ae8b58d4bdf75f391`。
- 构建缓存版本 `b027c55591f5d390`，9 个静态文件；发布产物扫描未发现三项高德凭据。没有部署查店后端或上传 `.env`。
- Chromium 验证 320/390/1280px 无横向溢出、随机抽取、减少动画、service worker 缓存后离线重开抽取；页面异常 0，高德或业务 API 请求 0。
- 公开版手机渲染记录位于本地 `test-results/public-mobile.png`，未纳入仓库。公开版保留原有随机规则；以下 68 项是本地完整版已有验证结果，并非新增公开版测试计数。
- 托管平台返回发布成功；未将此结果描述为中国大陆各运营商、微信内置浏览器或手机真机访问均已验证。

**UI 与逻辑已验证；真实高德本地接口联调已通过。** SDK 定位链路使用模拟 GPS 输入，地点与门店来自真实接口；尚不代表手机实际 GPS、真机导航或公网部署已经通过。

## 环境与结果

| 项目 | 实际结果 |
| --- | --- |
| 系统/运行时 | Windows，PowerShell，Node.js 24.12.0，npm 11.18.0 |
| 浏览器工具 | Playwright 1.63.0，Chromium 153.0.8010.12（无头浏览器） |
| 高德凭据 | 已将用户提供的三项凭据写入本地 `.env`，该文件被 `.gitignore` 排除；测试报告不记录凭据 |
| 依赖 | `npm install` 完成，锁文件已生成；当次 npm 审计报告 0 vulnerabilities |
| 单元与 HTTP 代理 | `npm test`：45 项通过，0 失败、0 跳过 |
| 浏览器验收 | `npm run test:e2e`：23 项通过，0 失败，最终运行 19.7 秒 |
| 生产构建 | `npm run build` 成功；缓存版本 `1bb5f4ea5566cf98`，15 个静态缓存资源 |
| 构建检查 | `npm run check:build` 成功，缓存资源存在、PNG 尺寸正确，无服务端凭据变量或测试注入 |
| 语法 | `src/*.js` 与 `server/*.js` 的 Node 语法检查通过 |
| 本地服务 | `http://127.0.0.1:4173/` 实际返回 HTTP 200；浏览器验收使用 4174 的生产构建预览 |

合计 **68 项自动化测试通过**。回归测试涉及的餐馆名称、坐标及响应为测试夹具；它们只在 `tests/` 中使用，不进入生产资源。HTTP 代理回归测试的高德上游采用可注入的模拟响应；浏览器回归预览强制空凭据，避免意外使用本地 `.env`。下面的真实联调独立执行，不计入这 68 项。

## 真实高德联调（2026-09-24）

- 本地配置接口确认 JS 与 Web服务均已配置；未输出任何密钥值。
- 真实 JS API 2.0、Geolocation 插件、serviceHost 坐标转换通过；应用成功接受定位结果，无脚本异常、无 CSP 违规。浏览器输入为公开地标的模拟 GPS，未使用用户实际位置。
- 坐标转换代理返回 HTTP 200；在内存中核对 SDK 坐标与代理转换结果相等，且与原始 GPS 不同，不记录精确坐标。
- 地点搜索以“北京市北京大学”为公开测试地点，返回 20 个真实候选；在浏览器选点后，周边 1km 重庆小面检索得到 4 家有效相关候选，`partial: false`，导航目标坐标均有效。
- 逆地理编码返回 HTTP 200 且地址非空；上述查询均使用当前真实凭据和高德上游。
- 已修复三项联调差异：CSP 允许当前 SDK 必需的动态代码执行及插件域名；代理补齐 JS 平台标记；定位适配兼容 `html5` 明确转换成功但 `isConverted: false` 的返回。转换失败、缺失证据或非 html5 来源仍拒绝进入兼容分支，新增回归测试通过。
- 真实服务端凭据参与构建校验：静态产物中没有安全密钥或 Web服务 Key。公开 JS Key 由配置接口在运行时提供。

## 核心逻辑与竞态覆盖

- 数据：日常 56、特别 8、唯一稳定 ID、所有搜索映射非空且最多 3 步，无统一“餐饮/美食/快餐”兜底。
- 概率：0.95 边界、每个池内等宽区间、允许重复、拒绝非法测试随机源；10 万次固定种子抽取作辅助统计，不要求每 20 次恰有一次特别项。
- 动画：快速连点只采样一次，恰好两次随机源调用；过程名称不写入 live region；减少动态效果直接出结果。
- 存储：无权限、坏 JSON、缺失/越界字段、写入失败；只保存设置和最后可用中心，不保存食物/门店/行为历史。
- 位置：拒绝定位保留缓存与时间；自动定位仅一次；主动点击才能重试；粗略/未知/相对半径精度不足要求确认；未转换坐标拒绝；逆编码失败不否定已得到的坐标。
- 竞态：改食物、中心或半径会中止旧请求；不能取消的晚到结果或错误也不能覆盖新状态；晚到定位和地址不能覆盖手动中心。
- 地点输入：300ms 防抖（逻辑测试缩短延时），输入变化立即使旧响应失效，关闭弹层取消；必须选择有坐标候选，文字本身不改变位置。
- 查询流程：不足 3 家才继续，达到 3 家停止，最多 3 步；按 ID 去重、合并有效字段，同名分店保留，缺坐标/非餐饮/越界结果过滤。
- 排序：specific 优先；related 提示；无效评分不展示，中位数仅内部使用，整层无评分重归一化；同分稳定排序；最多 6 家。
- 零结果/失败：成功无候选才是零结果；固定四档、用户点击扩大、5km 止；网络/凭据/权限/配额/超时不冒充零结果，后续失败保留部分结果并明确提示。
- 导航：选中门店的目的地、名称编码、GCJ-02 参数、App 尝试和网页入口；链接不含手动搜索中心作为起点、不指定出行方式、不自动打开。

## 代理与产物安全覆盖

- 服务端从 foodId 和计划步数取关键词；固定 POI 2.0、餐饮类型、综合排序、商业字段、20 条/第一页，不接受任意关键词/URL/分页覆盖。
- 坐标、半径、输入长度、HTTP 方法、跨站请求、未知路径和文件访问边界校验。
- `/api/config` 仅包含公开 JS Key 与状态；没有服务端密钥，接口错误不回显上游 info。
- JS 代理仅允许单坐标转换，固定高德上游；安全密钥服务端注入；JSONP 回调格式限制，不透传上游脚本。
- 高德 10001、10003、10012、10015、10016 错误分类，以及响应超时、坏 JSON、网络错误。
- 单 IP / 全局分钟 / 全局日预算、窗口恢复；默认不信任伪造的 `X-Forwarded-For`。
- 第三方名称如 `<img …>` 作为文字显示，未创建 HTML 图片或执行事件。
- 产物白名单、资源完整性、192/512px 图标、前端无服务端配置变量/测试注入；已加载实际 `.env` 检查构建不包含服务端凭据值。

## 浏览器与视觉检查

实际运行 **320、375、390、430、1280px** 五档宽度。每档检查初始页面、长餐食名/长店名/长地址/营业时间、展开列表、错误提示、位置弹层，均无横向溢出。截图来自实际 Chromium 渲染；人工查看了手机、极窄弹层与桌面代表截图。

已验证键盘 Tab 焦点循环、Escape 关闭、关闭后归还触发焦点、文本节点渲染、减少动画偏好，以及 200% 根字体设置下的操作与宽度。视口高度缩至 400px 后，正在输入的搜索框能滚动到弹层可见区域；这是软键盘导致视口缩小的模拟，**不是真机软键盘测试**。

首次浏览器运行暴露了两个实际问题：原生弹层 Tab 可能进入浏览器外部焦点；`type=search` 会先消费 Escape。增加显式焦点循环和 Escape 捕获后复测通过。渲染检查还促使补充视口缩小时滚动当前输入框的处理。最终版本无未解决的自动化失败。

可重生成的截图：

截图为本地测试生成物，未纳入仓库；运行浏览器测试可重新生成：

- 320px：`test-results/layout-320-initial.png`、`layout-320-results.png`、`layout-320-dialog.png`
- 375px：`test-results/layout-375-initial.png`、`layout-375-results.png`
- 390px：`test-results/layout-390-initial.png`、`layout-390-results.png`
- 430px：`test-results/layout-430-initial.png`、`layout-430-results.png`
- 桌面：`test-results/layout-1280-initial.png`、`layout-1280-results.png`

截图里的“测试门店/测试搜索中心”明确来自自动化测试夹具，不能用作真实查店证据。`test-results/` 会在下一次浏览器测试时重新生成，并已加入 `.gitignore`。

## PWA 检查

- 通过真实生产构建安装 service worker，缓存后切到离线、重载页面，仍可抽取具体餐食。
- 缓存中存在固定食物数据；没有 API、高德域名或带 Key 的请求。
- 离线查地点提示需要联网；已展开门店在断网时清空，不当作实时列表继续展示。
- worker 激活清除旧版本应用缓存，保留其他应用的缓存。
- 没有测试首次从未加载应用的设备离线打开，也不做此承诺。

## 尚未验证与所需条件

| 未验证项 | 最小所需条件 |
| --- | --- |
| 正式域名上的 JS API、serviceHost、CSP 调用链 | 已验证本地调用；部署到实际 HTTPS 域名后需再确认域名限制 |
| 手机实际 GPS、拒绝/超时/精度 | 支持定位的实际手机与浏览器，合适位置及权限；真实转换接口已联调 |
| 不同地区与餐食的实际召回、账户配额 | 本地公开地标查询已通过，仍需目标地区及账户控制台信息 |
| 评分量纲与真实商业字段覆盖 | 高德或实际账户接口的可核查 5 分制依据；目前评分默认关闭 |
| 高德 App 已装/未装，iOS、Android、微信内置浏览器的跳转 | 真机、高德 App 与公网 HTTPS 页面；网页入口已构造但未逐设备验证 |
| PWA 真机安装、操作系统安全区、系统字体/缩放、真实软键盘 | iOS Safari / Android Chrome 等实际设备 |
| 屏幕阅读器的实际声音播报 | VoiceOver / TalkBack 等实际辅助技术；目前只验证 ARIA 与最终文本更新 |
| Firefox/WebKit 的浏览器兼容性 | 对应浏览器测试环境；目前只实际运行 Chromium |
| 公网部署、TLS、systemd/Caddy、国内地区可达性 | 自有服务器、域名、备案条件及配置；未购买或开通资源 |

复现命令及后续真机验收步骤见 [README](README.md)。自动化模拟、真实接口联调与真机验证分别记录。
